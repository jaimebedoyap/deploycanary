apiVersion: apps/v1
kind: Deployment
metadata:
  name: nginx-blue
  namespace: bluegreen-demo
  labels:
    app: nginx
    slot: blue       # Identifica este ambiente como "blue"
spec:
  replicas: 2
  selector:
    matchLabels:
      app: nginx
      slot: blue
  template:
    metadata:
      labels:
        app: nginx
        slot: blue   # ← El Service apunta a este label para activar Blue
    spec:
      containers:
      - name: nginx
        image: nginx:1.25
        ports:
        - containerPort: 80
        volumeMounts:
        - name: html
          mountPath: /usr/share/nginx/html
        resources:
          requests:
            cpu: "50m"
            memory: "64Mi"
          limits:
            cpu: "100m"
            memory: "128Mi"
        livenessProbe:
          httpGet:
            path: /
            port: 80
          initialDelaySeconds: 5
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /
            port: 80
          initialDelaySeconds: 3
          periodSeconds: 5
      volumes:
      - name: html
        configMap:
          name: nginx-blue-html
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: nginx-blue-html
  namespace: bluegreen-demo
data:
  index.html: |
    <!DOCTYPE html>
    <html>
    <head>
      <meta charset="UTF-8">
      <title>Blue Environment</title>
      <style>
        * { margin: 0; padding: 0; box-sizing: border-box; }
        body {
          font-family: Arial, sans-serif;
          display: flex;
          justify-content: center;
          align-items: center;
          height: 100vh;
          background: linear-gradient(135deg, #1a73e8, #0d47a1);
          color: white;
        }
        .card {
          text-align: center;
          padding: 50px 60px;
          background: rgba(255,255,255,0.12);
          border-radius: 20px;
          border: 2px solid rgba(255,255,255,0.25);
          backdrop-filter: blur(10px);
          box-shadow: 0 8px 32px rgba(0,0,0,0.3);
        }
        .circle {
          width: 80px;
          height: 80px;
          background: white;
          border-radius: 50%;
          display: flex;
          align-items: center;
          justify-content: center;
          margin: 0 auto 20px;
          font-size: 2em;
        }
        h1 { font-size: 3em; margin-bottom: 8px; }
        .version { font-size: 1.1em; opacity: 0.85; margin-bottom: 20px; }
        .badge {
          display: inline-block;
          background: white;
          color: #1a73e8;
          padding: 8px 28px;
          border-radius: 25px;
          font-weight: bold;
          font-size: 1em;
          letter-spacing: 1px;
          margin-bottom: 20px;
        }
        .info {
          font-size: 0.85em;
          opacity: 0.75;
          margin-top: 16px;
          border-top: 1px solid rgba(255,255,255,0.2);
          padding-top: 16px;
        }
      </style>
    </head>
    <body>
      <div class="card">
        <div class="circle">🔵</div>
        <h1>Blue</h1>
        <p class="version">Ambiente Activo — nginx v1.25</p>
        <div class="badge">ACTIVE</div>
        <div class="info">
          <p>Blue/Green Deployment — GKE</p>
          <p>Slot: Blue | Versión: 1.0</p>
        </div>
      </div>
    </body>
    </html>