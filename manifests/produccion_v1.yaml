apiVersion: apps/v1
kind: Deployment
metadata:
  name: web-v1
spec:
  replicas: 3
  selector:
    matchLabels:
      app: mi-web
      version: v1
  template:
    metadata:
      labels:
        app: mi-web
        version: v1
    spec:
      containers:
      - name: web
        image: nginx:1.21
        ports:
        - containerPort: 80
        resources:
          requests:
            cpu: "250m"      # Antes tenías 500m
            memory: "512Mi"  # Antes tenías 2Gi
          limits:
            cpu: "250m"
            memory: "512Mi"